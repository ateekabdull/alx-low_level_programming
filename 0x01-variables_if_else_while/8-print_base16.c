#include <stdio.h>

/**
 * main - ddd
 *
 * Return: dfmlf
 */
int main(void)
{
	int hex;
	int dec;

	for (hex = '0'; hex <= '9'; hex++)
		putchar(hex);
	for (dec = 'a'; dec <= 'f'; dec++)
	putchar('\n');

	return (0);
}
