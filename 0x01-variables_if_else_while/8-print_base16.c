#include <stdio.h>

/**
 * main - ddd
 *
 * Return: dfmlf
 */
int main(void)
{
	char hex;
	char dec;

	for (hex = '0'; hex <= '9'; hex++)
		putchar(hex);
	for (dec = 'a'; dec <= 'f'; dec++)
	putchar('\n');

	return (0);
}
