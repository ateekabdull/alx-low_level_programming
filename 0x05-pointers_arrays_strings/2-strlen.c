#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: char pointer
 *
 * Return: 0
 */
int _strlen(char *s)
{
	int length, i;

	for (i = 0; s[i] != '\0'; i++)


	length = i;



	return (length);
}
