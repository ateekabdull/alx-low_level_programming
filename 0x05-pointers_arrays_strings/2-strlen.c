#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: char pointer
 *
 * Return: 0
 */
int _strlen(char *s)
{
	int length, i;

	for (i = 0; i < s; i++)


	length = i;



	return (length);
}
