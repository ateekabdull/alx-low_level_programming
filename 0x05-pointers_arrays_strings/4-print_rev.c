#include "main.h"

/**
 * print_rev - prints a string in reverse
 *
 * @s: the string
 *
 * Return: nothing
 */
void print_rev(char *s)
{
	int i, length, n;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	length = i;
	
	for (n = length - 1; n >= 0; i--)
		_putchar(s[n]);



}
